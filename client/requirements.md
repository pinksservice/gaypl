## Packages
framer-motion | For beautiful page transitions and micro-interactions
date-fns | For date formatting (Polish locale)
clsx | For conditional class names
tailwind-merge | For merging tailwind classes

## Notes
- Theme is "Dark Neon" with #00FF41 primary color.
- AI Chat uses the OpenAI integration endpoints (/api/conversations) directly, separate from the user-to-user chat in shared/schema.
- Mobile-first layout with bottom navigation.
- Polish language interface.
